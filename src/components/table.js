import { useState } from "react";
import "../App.css";

function Table({ info }) {
  console.log(info);
  return (
    <div className="App">
      <h2>Ella dog</h2>
    </div>
  );
}

export default Table;
